module.exports = {
    name: 'inferno-mid-long',
	description: 'Mid pop flash from Long. Useful to peek down T-Ramp.',
	aliases:  ['inf-mid-long'],
	execute(message, args) {
			if (!args.lenght){ 
			message.channel.send('')}}};